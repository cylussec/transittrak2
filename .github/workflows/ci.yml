name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Use npm 11.7.0 (workaround rollup optional dep bug)
        run: corepack use npm@11.7.0

      - name: Install
        run: npm ci

      - name: Install Rollup native binary (Linux workaround)
        run: npm install --no-save @rollup/rollup-linux-x64-gnu@4.54.0

      - name: DB Migrations Check
        run: npm run -w transittrack-worker db:check

      - name: Lint + Test + Build
        run: npm run ci
